// Módulo createApp (para poder criar a aplicação em vue)
import { createApp } from 'vue'

//Importar arquivo App.vue
import App from './App.vue'

//Renderizar (exibição das estruturas do arquivo [html, java])
createApp(App).mount('#app')
